import React from 'react';
import './Spinner.css'; // Create a CSS file for custom styles

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="loading-spinner"></div>
    </div>
  );
};

export default Spinner;
